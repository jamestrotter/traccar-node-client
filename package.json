{
  "name": "traccer-node-client",
  "version": "1.0.0",
  "description": "A node app that uses GPSD to send GPS updates to a Traccar instance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "haversine-distance": "^1.2.3",
    "node-gpsd-client": "^1.4.1",
    "tiny-json-http": "^7.5.1"
  },
  "devDependencies": {
    "nock": "^14.0.0-beta.11"
  }
}
